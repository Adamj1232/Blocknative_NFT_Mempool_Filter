import { InitializationOptions, Transaction, Account, Event, Unsubscribe, Destroy, Configuration } from './interfaces';
declare class Blocknative {
    private _storageKey;
    private _connectionId;
    private _dappId;
    private _system;
    private _networkId;
    private _appName;
    private _appVersion;
    private _transactionHandlers;
    private _socket;
    private _connected;
    private _sendMessage;
    private _watchedTransactions;
    private _watchedAccounts;
    private _configurations;
    private _pingTimeout?;
    private _heartbeat?;
    private _destroyed;
    private _onerror;
    private _queuedMessages;
    private _limitRules;
    private _waitToRetry;
    private _processingQueue;
    private _processQueue;
    transaction: Transaction;
    account: Account;
    event: Event;
    unsubscribe: Unsubscribe;
    destroy: Destroy;
    configuration: Configuration;
    constructor(options: InitializationOptions);
}
export default Blocknative;
